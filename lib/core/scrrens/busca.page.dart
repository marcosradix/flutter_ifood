import 'package:flutter/material.dart';

class BuscaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
      return Container(
        child: Center(
          child: Text("Tela de busca", style: TextStyle(fontSize: 20),),
        ),
      );
  }
}